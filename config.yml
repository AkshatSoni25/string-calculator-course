title: String Calculator
description: >-
  As a task by Incubyte, create a 'String Calculator' based on provided steps
  and conditions.
template:
  repo: string-calculator-template
  name: string-calculator
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
